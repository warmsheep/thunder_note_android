<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/mainContentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/border" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:minHeight="48dp"
        android:background="@color/nav_background"
        android:elevation="0dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:clipToPadding="false"
        app:elevation="0dp"
        app:activeIndicatorLabelPadding="0dp"
        app:itemActiveIndicatorStyle="@style/Widget.FlashNote.BottomNavigation.NoIndicator"
        app:itemIconSize="20dp"
        app:itemIconTint="@color/tab_text_colors"
        app:itemPaddingBottom="0dp"
        app:itemPaddingTop="0dp"
        app:itemTextColor="@color/tab_text_colors"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/menu_bottom_tabs" />

</LinearLayout>
